1️⃣ Diagnosis / Treatment 헤더가 너무 빨리 불투명해지는 문제
✔ 네 결론: 맞음 (정확)

setupSubHeroScrollEffect()가 scrollY 기준으로 판단
pin 중에는 scrollY ≠ 시각적 위치

이건 완전히 맞는 진단입니다.

🔧 보완해야 할 핵심 포인트 (중요)

지금 결론에서 한 단계 더 정리하면 이렇게 됩니다.

❌ 피해야 할 구조

전역 scroll listener에서 헤더 상태 제어

scrollY 기반 판단

pin 구간에서도 동일 로직 사용

✅ 권장 구조 (우선순위)

① ScrollTrigger가 있는 섹션

헤더 상태는 그 ScrollTrigger 내부에서만 관리

onEnter / onLeave / onEnterBack / onLeaveBack 사용

② ScrollTrigger가 없는 일반 섹션

IntersectionObserver 또는 sentinel 요소

③ 절대 하지 말 것

scrollY 기준으로 “섹션을 벗어났는지” 판단

📌 중요한 보완

“ScrollTrigger의 onLeave로만 판단”
→ onLeave + onLeaveBack 둘 다 필수

그래야 위로 스크롤할 때도 헤더가 정확히 복구됩니다.

2️⃣ Diagnosis 장비 스크롤이 2번에서 멈추는 문제
✔ 네 결론: 대체로 맞음, 하지만 한 가지 추가 원인 있음
맞는 부분

end 값 부족

pin 상태에서 스크롤 범위 종료

카드 개수 대비 scroll 거리 부족

⚠ 추가로 반드시 봐야 할 원인 (중요)

end 값 부족 + snap 또는 scrub 구조 충돌

많은 경우 실제 원인은 이 조합입니다:

pin + scrub + snap + 짧은 end


이 조합에서는:

사용자는 스크롤을 내리는데

ScrollTrigger는 “이미 끝났다”고 판단

브라우저는 pin 때문에 스크롤을 더 안 내려줌

마우스로 드래그하면 강제로 내려가는 현상 발생

🔧 보완 권장안
const panels = gsap.utils.toArray('.equipment-panel');

ScrollTrigger.create({
  trigger: '.horizontal-scroll-outer',
  start: 'top top',
  end: () => `+=${panels.length * window.innerHeight}`,
  scrub: true,
  pin: true,
  snap: false // ← 반드시 제거하거나 매우 약하게
});


📌 핵심

end는 패널 개수 기반 동적 계산

snap은 최소화 또는 제거

3️⃣ 전체 사이트 가로 스크롤 문제
✔ 네 결론: 완전히 맞음

여기서는 수정할 게 거의 없습니다.
다만 한 가지 더 체크해야 할 포인트가 있습니다.

🔧 추가 확인 포인트
.pin-spacer {
  width: 100% !important;
  overflow: hidden;
}


GSAP pin-spacer는 가끔 inline style로 width를 잘못 계산합니다.
특히 transform이나 scale이 섞이면 확률이 높아집니다.

📌 최종 체크리스트:

100vw 제거

body overflow-x hidden

pin-spacer width 확인

4️⃣ Treatment 흰 화면 + 인트로 배경 중복 문제
✔ 네 결론: 거의 완벽

특히 이 부분:

페이지 전환 시 ScrollTrigger kill 안 됨
pinSpacing true로 spacer 삽입
visibility:hidden으로 빈 공간 발생

이 조합을 짚은 건 아주 정확합니다.

🔧 보완해야 할 결정 포인트
A안 (가장 안정적, 추천)

pinSpacing: false

다음 섹션에 padding-top: 300vh 등 수동 처리

spacer 자체를 없애기

B안 (차선책)

pinSpacing 유지

.pin-spacer에 명시적 배경색

visibility:hidden 대신 opacity + pointer-events

📌 절대 피해야 할 것

visibility: hidden;


→ pin + visibility hidden = 흰 화면 제조기